# Python
__pycache__/
*.pyc
.venv/
venv/
.env
backend/.env
# Ruedas para instalación sin internet (se copian a mano, no por git)
backend/wheels/
# Ejecuciones persistidas (trazas JSON)
backend/runs/

# NOTA: frontend/out/ (build estático) NO se ignora a propósito: se construye en una
# máquina con Node y se transporta junto al repo a la máquina de destino.

# Node / Next.js
node_modules/
.next/
# (frontend/out/ se versiona a propósito: es el build estático que sirve FastAPI en la máquina de destino)
frontend/.env
frontend/.env.local
*.tsbuildinfo
next-env.d.ts

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/
